# 📖 คู่มือการใช้งานระบบ Draft2Desk (User Manual)

ยินดีต้อนรับสู่คู่มือการใช้งานระบบ **Draft2Desk - Smart Document Drafting Add-in** สำหรับ Microsoft Word

---

## 📌 สารบัญ (Table of Contents)
1. [การเปิดใช้งาน Add-in ใน Microsoft Word](#1-การเปิดใช้งาน-add-in-ใน-microsoft-word)
2. [การสร้างและจัดการเทมเพลตร่างเอกสาร](#2-การสร้างและจัดการเทมเพลตร่างเอกสาร)
3. [การใส่และการใช้งานตัวแปร (Variables)](#3-การใส่และการใช้งานตัวแปร-variables)
4. [การจัดรูปแบบเนื้อหาด้วยแถบเครื่องมือ](#4-การจัดรูปแบบเนื้อหาด้วยแถบเครื่องมือ)
5. [การแทรกข้อความลงในเอกสาร Word](#5-การแทรกข้อความลงในเอกสาร-word)
6. [การตั้งค่าฟอนต์และขนาดตัวอักษร](#6-การตั้งค่าฟอนต์และขนาดตัวอักษร)
7. [การสำรองข้อมูลและการย้ายไปเครื่องอื่น](#7-การสำรองข้อมูลและการย้ายไปเครื่องอื่น)

---

## 1. การเปิดใช้งาน Add-in ใน Microsoft Word
1. เปิดโปรแกรม **Microsoft Word**
2. ไปที่แถบ **แทรก (Insert)** บน Ribbon ด้านบน
3. คลิกปุ่ม **ส่วนเติมเต็มของฉัน (My Add-ins)** -> เลือก **Draft2Desk**
4. หน้าต่าง Taskpane ด้านขวาจะแสดงหน้าจอระบบ Draft2Desk พร้อมใช้งาน

---

## 2. การสร้างและจัดการเทมเพลตร่างเอกสาร

### ➕ การสร้างเทมเพลตใหม่:
1. คลิกปุ่มวงกลมบวก **`+` (FAB Button)** ด้านขวาล่างของหน้าจอ
2. พิมพ์ **ชื่อเทมเพลต** (เช่น *บันทึกเสนอขออนุมัติหลักการ*)
3. เลือก **หมวดหมู่** (เช่น *บันทึกข้อความ*, *คำสั่ง*, *หนังสือภายนอก*)
4. พิมพ์หรือจัดวางเนื้อหาเทมเพลตในช่องเนื้อหา
5. กดปุ่ม **"บันทึกเทมเพลต"**

### ✏️ การแก้ไข/ลบเทมเพลต:
* **แก้ไข:** คลิกไอคอนรูปดินสอ ✏️ บนการ์ดเทมเพลตที่ต้องการ
* **ลบ:** คลิกไอคอนรูปถังขยะ 🗑️ -> ระบบจะขึ้นหน้าต่างยืนยัน ให้กด "ลบเทมเพลต"

---

## 3. การใส่และการใช้งานตัวแปร (Variables)

ตัวแปรช่วยให้คุณสร้างฟิลด์สำหรับกรอกข้อมูลเติมคำในช่องว่างก่อนแทรกลง Word

### ประเภทของตัวแปรในระบบ:
1. **ตัวแปรข้อความทั่วไป (`{{ชื่อตัวแปร}}`):**
   * สำหรับข้อความที่ต้องพิมพ์เปลี่ยนค่า เช่น `{{เรื่อง}}`, `{{เรียน}}`, `{{วันที่}}`
2. **ตัวแปรตัวเลือก Radio Button (`{{opt:ชื่อตัวแปร}}`):**
   * สำหรับตัวเลือกปุ่มวิทยุประเภท **มี / ไม่มี** เช่น `{{opt:มีเอกสารแนบ}}` หรือ `{{opt:ด่วนที่สุด}}`
   * หากเลือก **`🔘 มี`** ➔ แทรกลงใน Word เป็นคำว่า **`มีเอกสารแนบ`**
   * หากเลือก **`⚪ ไม่มี`** ➔ ไม่ใส่คำนั้นลงไปในเอกสาร (ลบตัวแปรออกเป็นช่องว่างเปล่า)

### วิธีการใส่ตัวแปรลงในเนื้อหา:
* **วิธีที่ 1 (ปุ่มด่วน - แนะนำ):** คลิกปุ่ม **`+ ตัวแปร`** บนแถบเครื่องมือ ➔ ดรอปดาวน์เลือก **`ข้อความ T`** หรือ **`มี/ไม่มี (Radio 🔘)`** ➔ พิมพ์ชื่อตัวแปรแล้วกด Enter
* **วิธีที่ 2 (พิมพ์เอง):** พิมพ์ปีกกาคู่ครอบชื่อตัวแปร เช่น `{{วันที่}}` หรือ `{{opt:มีเอกสารแนบ}}`

### การกรอกค่าตัวแปรเมื่อใช้งาน:
1. คลิกปุ่ม **"ใช้งาน"** บนการ์ดเทมเพลต
2. แถบสไลด์ด่วนจากด้านล่างจะเลื่อนขึ้นมาพร้อมช่องกรอกตัวแปรและตัวเลือกแบบ Radio Button
3. กรอกข้อมูลและเลือกตัวเลือก ➔ กด **"แทรกเนื้อหาลง Word"**

---

## 4. การจัดรูปแบบเนื้อหาด้วยแถบเครื่องมือ

หน้าต่างเขียนเทมเพลตมีแถบเครื่องมือช่วยจัดรูปแบบ:
* **`B` (ตัวหนา):** สัญลักษณ์ตัวหนา (`<b>`)
* **`I` (ตัวเอียง):** สัญลักษณ์ตัวเอียง (`<i>`)
* **`U` (ขีดเส้นใต้):** ขีดเส้นใต้ตัวอักษร (`<u>`)
* **`H1` / `H2` (หัวข้อ):** ขนาดหัวข้อเรื่อง (`<h3>`, `<h4>`)
* **`•` (รายการจุด):** สัญลักษณ์แสดงหัวข้อย่อย (`<ul>`)
* **`1.` (รายการตัวเลข):** ลำดับตัวเลข (`<ol>`)
* **`田` (ตาราง):** แทรกลาราง 2x2
* **`⇥` (เว้นย่อหน้า):** 
  * หากคลิกโดยไม่เลือกข้อความ: แทรกระยะย่อหน้า 8 ช่องว่าง (`&nbsp;`) ณ จุดพิมพ์
  * หากเลือกข้อความก่อน: จัดข้อความนั้นเป็นย่อหน้าเยื้องซ้าย (`margin-left: 2em`)

---

## 5. การแทรกข้อความลงในเอกสาร Word

เมื่อกด **"แทรกเนื้อหาลง Word"**:
* เนื้อหาจะถูกแทรกลงในตำแหน่งที่เคอร์เซอร์ของ Word วางอยู่
* ข้อความจะถูกบรรจุใน **กล่องข้อความลอย (Floating Text Box)** ที่:
  - **ไม่มีสีพื้นหลัง (Transparent):** มองเห็นพื้นหลังกระดาษทะลุผ่าน
  - **ไม่มีเส้นขอบ (Borderless):** ไร้กรอบสี่เหลี่ยมรบกวน
  - **ยืดหยุ่นสูง:** สามารถคลิกจับลากเคลื่อนย้ายไปมาบนหน้าเอกสาร Word ได้อย่างอิสระ
  - **ปรับความสูงอัตโนมัติ:** ความสูงของกล่องจะยืดตามความยาวเนื้อหาโดยอัตโนมัติ

---

## 6. การตั้งค่าฟอนต์และขนาดตัวอักษร

1. คลิกไอคอนฟันเฟือง **`⚙️`** ด้านขวาบนของแอดอิน
2. เลือก **รูปแบบฟอนต์หลัก (Font Family)** เช่น `TH Sarabun New`, `Angsana New`, `Kanit`, `Tahoma`
3. เลือก **ขนาดตัวอักษร (Font Size)** เช่น `16pt` (ขนาดมาตรฐานหนังสือราชการ)
4. ดูตัวอย่างข้อความในกล่อง **Live Preview**
5. กด **"บันทึกการตั้งค่า"** (ระบบจะจำค่านึ้ไว้ตลอดการใช้งาน)

---

## 7. การสำรองข้อมูลและการย้ายไปเครื่องอื่น

* **ไฟล์เก็บข้อมูลทั้งหมด:** อยู่ที่ `backend/draft2desk.db`
* **การสำรอง/ย้ายเครื่อง:** เพียงก๊อปปี้ไฟล์ `draft2desk.db` ไปวางในโฟลเดอร์ `backend/` ของเครื่องใหม่ ข้อมูลเทมเพลตทั้งหมดจะย้ายไปพร้อมใช้งานทันที

---

## 8. การถอนการติดตั้ง (Uninstallation)

หากไม่ต้องการใช้งาน Draft2Desk ใน Microsoft Word สามารถถอนการติดตั้งได้ง่ายๆ ดังนี้:

### 🍏 บน macOS:
* ดับเบิลคลิกไฟล์ **`scripts/uninstall_mac.command`**
* ระบบจะทำการลบไฟล์ Manifest ออกจาก Word และปิดการทำงานของเซิร์ฟเวอร์ให้อัตโนมัติ

### 🪟 บน Windows:
* ดับเบิลคลิกไฟล์ **`scripts/uninstall_win.bat`**
* ระบบจะทำการลบไฟล์ Manifest ออกจากโฟลเดอร์ส่วนเติมเต็มของ Word และปิดเซิร์ฟเวอร์ให้อัตโนมัติ

> **หมายเหตุ:** การถอนการติดตั้งแบบสคริปต์จะไม่ลบไฟล์ข้อมูลเทมเพลต (`draft2desk.db`) ของคุณ หากต้องการกลับมาติดตั้งใช้งานอีกครั้ง ข้อมูลเดิมทั้งหมดจะยังคงอยู่เหมือนเดิมครับ
